
node {
    stage('Build') {
        if (env.NAME=="AMIT") {
            println("Build started")
        } else {
            println("Name is differrent")
        }
    }
    stage('Test') {
        println("test started")
    }
    stage('Deploy') {
        println("deploy started")
    }
}
